{
  "name": "@graphcalculator/server",
  "version": "1.0.0",
  "main": "dist/api/v1/server.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"nodemon --watch dist/api/v1 dist/api/v1/server.js\"  \"tsc -p tsconfig.server.json -w\"",
    "build": "tsc -p ./tsconfig.server.json",
    "clean": "rm -rf dist"
  },
  "author": "Iosub David",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.2",
    "uuid": "*"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "nodemon": "^3.1.9",
    "concurrently": "*"
  }
}
